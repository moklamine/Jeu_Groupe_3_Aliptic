{% extends 'base.html.twig' %}

{% block title %}Modifier mon pseudo - La Tour Galamadriabuyak{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1 class="text-center mt-5 mb-5">Modifier mon pseudo</h1>
    <a href="{{ path('account') }}">Retourner sur mon compte</a><br />
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}
</div>

{% endblock %}